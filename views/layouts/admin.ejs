<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">

    <title>Dashboard - NiceAdmin Bootstrap Template</title>
    <meta name="robots" content="noindex, nofollow">
    <meta content="This is a dashboard for IBC Tank Store" name="description">
    <meta content="ibctank ibctankstore ibc tank store ibc totes" name="keywords">

    <!-- Favicons -->
    <link href="/img/favicon.ico" rel="icon">
    <link href="/img/favicon.ico" rel="apple-touch-icon">

    <!-- Google Fonts -->
    <link href="https://fonts.gstatic.com" rel="preconnect">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Nunito:300,300i,400,400i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i"
        rel="stylesheet">

    <!-- Vendor CSS Files -->
    <link href="/admin/vendors/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link href="/admin/vendors/boxicons/css/boxicons.min.css" rel="stylesheet">
    <link href="/admin/vendors/remixicon/remixicon.css" rel="stylesheet">
    <!-- Template Main CSS File -->
    <link href="/admin/assets/css/style.css" rel="stylesheet">
    <link href="/admin/assets/css/upload.css" rel="stylesheet">
    <%- include('../shared/_styles') %>
    <!-- Google AdSense -->
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-5595834937390441"
       crossorigin="anonymous"></script>
</head>

<body>
    <%- include('../admin/partials/_header') %>
    <%- include('../shared/_alerts') %>
    <%- include('../admin/partials/_sidebar') %>
    <main id="main" class="main">
        <%- include('../admin/partials/_breadcrumb') %>
        <section class="section min-vh-100">
            <%- body %>
        </section>
    </main>
    <%- include('../admin/partials/_footer') %>
    <a href="#" class="back-to-top d-flex align-items-center justify-content-center">
        <i class="bi bi-arrow-up-short"></i>
    </a>
    <!-- Vendor JS Files -->
    <script src="/admin/vendors/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="/admin/vendors/chart.js/chart.umd.js"></script>
    <script src="/admin/vendors/echarts/echarts.min.js"></script>
    <script src="/admin/vendors/jQuery/jquery-3.7.1.min.js"></script>
    <script src="/admin/vendors/datatables/dataTables.js"></script>
    <script src="/admin/vendors/php-email-form/validate.js"></script>
    <!-- Add this to your layout.ejs head section -->
    <link rel="stylesheet" href="/admin/vendors/datatables/dataTables.css">
    <!-- Template Main JS File -->
    <script src="/admin/assets/js/main.js"></script>
    <!-- Add this script in your admin layout or at the end of index.ejs -->
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Make table rows clickable
            const rows = document.querySelectorAll('.click-able');
            rows.forEach(cell => {
                // Prevent nested clicks from bubbling up
                cell.addEventListener('click', function (e) {
                    // Don't trigger if clicking on dropdown or within dropdown
                    if (e.target.closest('.dropdown') ||
                        e.target.closest('form') ||
                        e.target.tagName === 'A' ||
                        e.target.tagName === 'BUTTON') {
                        return;
                    }
                    const id = this.getAttribute('data-id');
                    const type = this.getAttribute('data-of');
                    if (id && type === 'products') {
                        window.location.href = `/admin/products/${id}`;
                    }
                });
            });
            // Add hover effect
            rows.forEach(cell => {
                cell.style.cursor = 'pointer';
                cell.closest('tr').addEventListener('mouseenter', function () {
                    this.style.backgroundColor = '#f8f9fa';
                });
                cell.closest('tr').addEventListener('mouseleave', function () {
                    this.style.backgroundColor = '';
                });
            });
        });
    </script>
    <%- include('../shared/_scripts') %>
    <%- include('../shared/_fancybox') %>
    <!-- Place the first <script> tag in your HTML's <head> -->
    <script src="https://cdn.tiny.cloud/1/6gcbfrf9aa0izw5eu8u60vno25oxthr44mslcumic8b6l7y2/tinymce/8/tinymce.min.js"
        referrerpolicy="origin" crossorigin="anonymous"></script>
    <script src="/admin/assets/js/tinymce.js"></script>
    <script type="application/json" id="product-images-data">
        <%- JSON.stringify(productImages || []) %>
    </script>
</body>

</html>